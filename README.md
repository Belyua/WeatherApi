# WeatherApi
```
usage: main.py [-h] [-d DATE] [-l LOCATION] [-o OUTPUT]
optional arguments:
  -h, --help            show this help message and exit
  -d DATE, --date DATE  Date (YYYY-MM-DD)
  -l LOCATION, --location LOCATION
                        Location
  -o OUTPUT, --output OUTPUT
                        Output file
